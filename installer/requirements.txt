# The production installer currently uses only the Python standard library.
# Keep this file as the authoritative runtime dependency list so future
# dependencies are synchronized into the repository-local .venv automatically.
